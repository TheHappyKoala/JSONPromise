# JSONPromise
Promise-based JSONP API

<h2>Usage</h2>

<code>

import JSONP from './JSONP';

<br/>

JSONP( 'myurl' )
  <br/>.then( ( data ) => alert( data ) )
  <br/>.catch( ( error ) => alert( error ) );

</code>
